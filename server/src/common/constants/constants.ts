export const WEEK_IN_MS = 7 * 24 * 60 * 60 * 1000;
export const PASSWORD_REGEX = /^(?=.*[A-Za-z])(?=.*\d)[A-Za-z\d]{8,32}$/;
export const USERNAME_REGEX = /^[A-Za-z0-9_-]{2,30}$/;
export const MINUTE = 1000 * 60;
export const HOUR = MINUTE * 60;
